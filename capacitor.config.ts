import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ar.com.matiasgaleano.presupuestos',
  appName: 'Generador de presupuestos',
  webDir: 'www/browser',
};

export default config;
